{
  "name": "@unicom/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@nestjs/core": "^11.0.0",
    "@unicom/config": "workspace:*",
    "pino": "^9.7.0"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "vitest": "^3.1.2"
  }
}
